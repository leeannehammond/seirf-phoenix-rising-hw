<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <h1>Logs index page</h1>
        <ul>
            <% for(let i = 0; i < logs.length; i++){ %>
                <li>
                    The <%=logs[i].name; %> is  <%=<logs i="" class="entry"></logs> %>.
                    <% if(logs[i].shipIsBroken === true){ %>
                        It is ready to eat
                    <% } else { %>
                        It is not broken
                    <% } %>
                </li>
            <% } %>
        </ul>
    </body>
</html>